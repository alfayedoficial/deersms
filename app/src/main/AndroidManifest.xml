<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alialfayed.deersms">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.FQRActivity"></activity>
        <activity android:name=".view.activity.TemplatesActivity" />
        <activity android:name=".view.activity.ContactsActivity" />
        <activity android:name=".view.activity.GroupContactsActivity" />
        <activity android:name=".view.activity.GroupNameActivity" />
        <activity android:name=".view.activity.AddGroupActivity" />
        <activity android:name=".view.activity.AddMessageActivity" />
        <activity android:name=".view.activity.ScheduleMessageActivity" />
        <activity android:name=".view.activity.ForgetPasswordActivity" />
        <activity android:name=".view.activity.SettingsActivity" />
        <activity android:name=".view.activity.ProfileActivity" />
        <activity android:name=".view.activity.HomeActivity" />
        <activity android:name=".view.activity.SignUpActivity" />
        <activity android:name=".view.activity.SignInActivity" />
        <activity android:name=".view.activity.StartActivity" />
        <activity android:name=".view.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".utils.SMSReceiver"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>